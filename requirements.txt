# =============================================================================
# InsightLoop - runtime dependencies
# Pinned to versions that support Python 3.13 / 3.14.
# =============================================================================

# --- Web framework ---------------------------------------------------------
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
python-multipart>=0.0.12,<1.0

# --- Data validation / config ---------------------------------------------
pydantic[email]>=2.9.0,<3.0
pydantic-settings>=2.6.0,<3.0

# --- Persistence ----------------------------------------------------------
SQLAlchemy>=2.0.36,<3.0

# --- Templates ------------------------------------------------------------
Jinja2>=3.1.4,<4.0

# --- LLM providers --------------------------------------------------------
openai>=1.54.0,<2.0
anthropic>=0.39.0,<1.0

# --- HTTP client (used by Ollama adapter) --------------------------------
httpx>=0.27.0,<1.0

# --- Data handling / exports ---------------------------------------------
pandas>=2.2.3,<3.0
reportlab>=4.2.5,<5.0
